apiVersion: v1
kind: Service
metadata:
  name: fighting-characters
  labels:
    app: fighting-characters
spec:
  type: NodePort
  selector:
    app: fighting-characters
  ports:
    - name: http
      port: 80
      targetPort: 8000   
